# CONTEXT.md

このファイルは、これまでチャット（Claude）上で行ってきた `RuLiLuRa Manager` の設計・実装の相談内容と決定事項を、Claude Code（あるいは別の開発環境）に引き継ぐためのサマリーです。

詳細な仕様は `SPEC.md`（v1.4.10）を正とします。このファイルは「なぜこうなっているか」「どういう経緯で決まったか」という背景・意図を補足するものです。

---

## 1. プロジェクト概要

**RuLiLuRa Manager** は、TRPG「幻奏戦記 Ru/Li/Lu/Ra ノイシュタルト」（HobbyJAPAN）のキャラクターシート管理Webシステム。

- 英雄・歌姫・奏甲の3種類のキャラクターシートを作成・編集・閲覧
- ユーザー認証（JWT）による個人データ管理
- 公開/非公開設定・共有URLによる閲覧共有
- **ccfolia**（オンラインセッションツール）用のキャラクターカードJSONをワンクリックで出力

### 技術スタック
- バックエンド: Node.js + Express
- DB: Turso（libSQL、@libsql/client）。ローカル開発時は環境変数`TURSO_URL`未設定でローカルSQLiteファイルにフォールバック
- フロントエンド: Vanilla JS（フレームワーク不使用、SPA、ページ遷移はJSで`currentPage`を切り替える自作ルーティング）
- デプロイ: Render（render.yaml設定済み）

### ファイル構成（主要ファイルのみ）
```
rulilura/
├── SPEC.md                 # 仕様書（正本、v1.4.10）
├── README.md
├── package.json
├── render.yaml
├── src/
│   ├── server.js
│   ├── db/database.js      # Turso/SQLite接続
│   ├── middleware/auth.js  # JWT認証
│   └── routes/
│       ├── auth.js
│       └── crud.js         # heroes/singers/armors共通CRUD
└── public/
    ├── index.html
    ├── css/style.css
    └── js/
        ├── gamedata.js     # スキル定数・ccfoliaコマンド生成（buildCcfoliaCommands等）
        ├── item-data.js    # 歌術アイテムカタログ（SONG_ITEMS_CATALOG）共通データ
        ├── api.js          # APIクライアント・認証状態
        ├── hero-form.js    # 英雄シートフォーム
        ├── singer-form.js  # 歌姫シートフォーム
        ├── armor-form.js   # 奏甲シートフォーム
        └── app.js          # メインアプリ・画面遷移・詳細表示・ccfolia出力
```

---

## 2. 開発の経緯（時系列の主な決定事項）

以下は会話の中で実際に行った主な変更を時系列で要約したもの。各項目の詳細仕様は `SPEC.md` の対応バージョン行（変更履歴テーブル）を参照。

1. **初版作成** — 英雄・歌姫・奏甲の基本シート、認証、公開/共有、ccfolia出力のMVPを構築。
2. **Turso移行** — better-sqlite3からTurso（@libsql/client）に変更。Windowsローカル環境のパス問題（dataフォルダ自動生成、絶対パス化）も対応。
3. **英雄能力の個別管理化** — テキスト自由記述だった英雄能力を、カタログ54種からのドロップダウン選択＋メモ欄の複数行管理に変更。
4. **奏甲の上級ルール対応** — 移動速度・武装タイプ・ハードポイント（機体全体）・命中部位表などを追加。
5. **歌姫能力・歌術の個別管理化** — 英雄能力と同様にカタログ化（歌姫能力63種、歌術18種）。歌術には各ランクの効果テキストも追加し選択時に自動表示。
6. **モーダル廃止 → ページ遷移方式** — UIがポップアップだと見づらいとの指摘から、詳細・編集・ccfolia出力を専用ページに変更。`_prevPage`という簡易履歴機構を実装（**ここで一度バグを作り、後に修正している。下記「ハマったポイント」参照**）。
7. **英雄・歌姫の防具個別管理化** — 防具をテキストから武器と同様の個別行（防具名・防御値・回避ペナルティ）に変更。
8. **回避修正の2段階化** — 「回避の元値」と「回避修正（10単位に切り捨てた値）」を分離。切り捨てルールは `Math.floor(value / 10) * 10`（負の値も0から離れる方向に切り捨て：-1〜-10は-10、-11〜-20は-20）。
9. **HPのベース値/ボーナス分離** — 能力値変更時にレベルアップ等で得たHPボーナスが消えてしまう不具合を修正。`hp.normal`等を「ベース値＋ボーナス」の合計として持ち、`normalBonus`等を別フィールドで保持。
10. **戻るボタンの不具合修正** — `_prevPage`の更新条件を「現在ページがmypage/publicのときのみ」に統一（詳細→編集→保存→詳細...と巡回しても一覧に戻れるよう修正）。
11. **奏甲武器の戦闘修正自動化** — 射程（白/1〜4）に応じて白兵修正・射撃修正を自動入力。ハードポイント（武器単位）・メモ欄も追加。
12. **奏甲の搭乗者選択（重要・仕様が二段階で変わった）**
    - 最初は「搭乗英雄」「搭乗歌姫」を**両方同時に**選べるUIで実装。
    - その後「奏甲に乗るのは英雄か、奏甲乗りを持つ歌姫のどちらか一人だけ」という指摘を受け、**単一の排他的選択**（`pilot_type` + `pilot_id`）に変更。歌姫の選択肢は`singer_abilities`に番号26（奏甲乗り）を持つものだけに絞り込む。
    - 選択した搭乗者のデータから「英雄の値」（白兵・射撃・回避・抵抗・偵察）を自動入力。武器のスキル欄も搭乗者の保有スキルからドロップダウン選択→値を自動取得。
13. **歌術アイテムの個別管理化** — 英雄・歌姫共通で使う`item-data.js`（カタログ48種）を新設。英雄・歌姫シートの「その他装備品」を「歌術アイテム（個別行）」と「一般アイテム（自由記述）」に分割。あわせて、歌姫の一般アイテム表示で**改行が反映されない不具合**（`<div>`で囲っていたのが原因）を `<pre style="white-space:pre-wrap">` に統一して修正。
14. **歌姫ccfolia出力の改善（複数回のやり取り）**
    - statusに負傷HPを追加。
    - チャットパレットが「ラベル行＋コマンド行」の2行で出力されていた不具合を**1行のみ**に修正（`commands.map(c => c.value).join('\n')` に統一。英雄・奏甲も同様に修正）。
    - paramsに各種消耗値（肉体/気力/絆）・絆獲得値を追加し、コマンドは`{肉体消耗値}`のようなパラメータ参照式に変更。
    - 最初「歌術ごとに」専用コマンドを生成していたが、「歌術それぞれにチャットパレットは不要、『歌術を使う時』の汎用コマンドが1つあればいい」との指摘で**歌術ごとのループを削除**し、「歌術判定」という単一の汎用コマンドのみに統一。
15. **歌術使用時の消耗値補正欄（最終仕様）**
    - 最初「能力・アイテムの効果から自動計算する」案で実装（カタログに`songCostMod`数値プロパティを追加するアプローチ）。
    - しかし「ユーザーが自分で計算して記載するための枠だけあればいい」との指示で**自動計算ロジックを完全に削除**し、`song_cost_mod`という手動入力の数値欄（補正値のみ）に変更。
    - 当初は「補正後の最終値」を入れる欄として作ったが、「補正後の消耗値じゃなくて補正だけを追加記入する欄に」という指示で**最終仕様は「補正値のみ」**（例: `+1`）。
    - ccfoliaの「歌術判定」コマンドは `2D10<={気力消耗値}{符号付き補正値} [...]` の形式で、基本消耗値パラメータに補正を加算する形に統一。

---

## 3. 重要な設計判断・ルール（実装時に再度ハマりやすい点）

### 3.1 回避修正の切り捨てルール
ユーザーから明確な指摘あり：「-1〜-10は-10、-11〜-20は-20（0ではない）」。
計算式は `Math.floor(value / 10) * 10` で正しく両方のケースに対応できる（正の値も負の値も同じ式でOK）。英雄・歌姫共通で `floorTo10()` というメソッド名で実装されている。

### 3.2 歌姫のゲージは4種類、消耗とゲインは別物
最初の実装は「肉体・気力・絆」の3ゲージに`cost`（消耗値）と`gain`（獲得値）を両方持たせる誤った構造だった。
正しくは：
- 消耗ゲージ：肉体・気力・絆（消耗値のみ）
- ゲインゲージ：絆のみ（獲得値のみ。消耗ゲージの「絆」とは別概念・別オブジェクト）

データ構造は `gauges` オブジェクトに `'肉体'`, `'気力'`, `'絆'`, `'絆ゲイン'` という4キーを持たせている。

### 3.3 HPはベース値とボーナスの合計
英雄・歌姫ともにHP（通常HP・負傷HP、英雄はMPも）は以下の3層構造：
- ベース値：能力値から自動計算（読み取り専用表示）
- ボーナス：レベルアップ・能力による恒久加算（手動入力・保持される）
- 合計：ベース値＋ボーナス（保存・表示用の最終値、`hp.normal`等として保存）

能力値を変更してもボーナスは消えないようにする、というのが修正の要点。`_recalcHP()` というメソッドで再計算している。

### 3.4 奏甲の搭乗者は「英雄」か「奏甲乗り歌姫」のどちらか一人のみ
両方同時に紐付けることはできない。データ上は `pilot_type`（`'hero'` | `'singer'` | `''`）と `pilot_id` の組で1人だけを表現する。DBの`hero_id`/`singer_id`カラムは選ばれなかった側を必ず`null`にする。

「奏甲乗り」は歌姫能力カタログの番号26。これを持たない歌姫は奏甲シートの搭乗者選択肢に出てこない。

### 3.5 ccfoliaのチャットパレットは「1行1コマンド」
ccfoliaのチャットパレット仕様は1行につき1コマンドのみ有効。`label`（コマンド名表示用の行）と`value`（実際のコマンド文字列）を分けて2行出力していたのは誤りで、`value`のみを1行ずつ出力するのが正しい。

`gamedata.js`内の `buildCcfoliaCommands`（英雄）・`buildSingerCcfoliaCommands`（歌姫）・`buildArmorCcfoliaCommands`（奏甲）はいずれも `{ value: '...' }` の配列を返し、呼び出し側（`app.js`）で `commands.map(c => c.value).join('\n')` として結合している。

### 3.6 歌術の補正は自動計算しない（意図的な仕様）
歌姫能力・歌術アイテムの効果テキストはカタログ上すべて自由文（`summary`/`effect`フィールド）であり、構造化されていないため、補正値を機械的に自動判定することは（少なくとも現状のデータ構造では）行っていない。

一度「カタログに数値プロパティを追加して自動算出する」アプローチを試みたが、**ユーザーの意向により撤回**し、最終的に「ユーザーが手動で補正値だけを記入する数値欄」（`song_cost_mod`）という、最もシンプルな実装に落ち着いている。今後この方針を変える場合は、ユーザーに確認すること。

---

## 4. 今後のClaude Codeでの作業時の注意点

- **`SPEC.md` を必ず先に読むこと。** このファイルはあくまで経緯の補足であり、フィールド名・型・計算式の正確な定義は`SPEC.md`にある。
- フォームファイル（`hero-form.js` / `singer-form.js` / `armor-form.js`）はいずれも `defaults()` → `renderForm()` → `attachAutoCalc()` → `collectForm()` という共通パターンで実装されている。新しいフィールドを追加する際はこの4箇所すべてに対応が必要（defaultsに初期値、renderFormにHTML、attachAutoCalcにイベント、collectFormに収集ロジック）。
- 英雄・歌姫・奏甲はそれぞれ独立したフォームファイルだが、共通のロジック（例: `floorTo10`、防具合計計算、HP再計算）が**ほぼ同じコードでファイルごとに重複実装されている**。リファクタリングの余地はあるが、現状は意図的に分離されたまま（共通化の指示は出ていない）。
- 旧データ形式（テキスト自由記述）からの移行は「自動変換しない」方針。`abilities_memo`や`songs`など旧フィールドが存在する場合は編集画面に警告を表示し、ユーザーが手動で新形式に移すUIになっている。この互換性コードを削除する場合はユーザーに確認すること。
- 作業中、サンドボックスのファイルが一度消失する事象が過去にあった。作業前に `/mnt/user-data/outputs/` に直近のtarballがあるか確認し、無ければ最初から構築し直す必要がある。

---

## 5. 現在のバージョン

- 仕様書: `SPEC.md` v1.4.10
- 最新の出力物: `rulilura-manager.tar.gz`（コード一式）, `RuLiLuRa_Manager_仕様書_v1.4.10.md`

両ファイルとも `/mnt/user-data/outputs/` に出力済み。
