# RuLiLuRa Manager システム仕様書

**文書番号**: RLMGR-SPEC-001  
**バージョン**: 1.4.0  
**作成日**: 2025年6月  
**最終更新日**: 2025年6月  
**対象システム**: 幻奏戦記 Ru/Li/Lu/Ra ノイシュタルト キャラクター管理Webシステム

---

## 変更履歴

| バージョン | 更新日 | 変更内容 |
|-----------|--------|---------|
| 1.0.0 | 2025年6月 | 初版作成 |
| 1.1.0 | 2025年6月 | DBをbetter-sqlite3からTurso（@libsql/client）に変更。環境変数`TURSO_URL`/`TURSO_AUTH_TOKEN`追加。Windowsローカル環境でのSQLiteパス絶対パス化・dataフォルダ自動生成対応 |
| 1.2.0 | 2025年6月 | 英雄能力をテキスト一括入力から個別行管理（54種ドロップダウン選択＋メモ欄）に変更。旧形式`abilities_memo`との後方互換を保持。奏甲シートに上級ルール管理項目（移動速度・移動属性・荷重管理・武装タイプ・ハードポイント・価格/維持費・命中部位表）を追加 |
| 1.3.0 | 2025年6月 | 歌姫能力をテキスト一括入力から個別行管理（63種ドロップダウン選択＋メモ欄）に変更。歌術を個別行管理（18種選択＋ランク選択＋メモ欄）に変更。旧形式`abilities_memo`/`songs`との後方互換を保持。戦闘系歌姫の武器・装備欄を追加。ccfolia JSON出力の歌姫コマンドに歌術・武器攻撃コマンドを追加 |
| 1.3.1 | 2025年6月 | 歌姫スキルを18種から全36種（英雄と同一リスト）に拡張。歌術カタログに各ランクの効果テキストを追加しフォーム上で選択時に自動表示するよう変更 |
| 1.4.0 | 2025年6月 | モーダルポップアップを廃止し全画面ページ遷移方式に変更（詳細・編集・ccfolia出力を専用ページ化）。英雄シートの防具をテキスト自由記述から武器と同様の個別行管理に変更し、防御値・回避ペナルティの合計を自動算出。回避修正の算出ルールを「元値」と「10単位に切り捨てた回避修正」の2段階に分離し、両方をシート上に表記するよう修正 |

---

## 目次

1. [システム概要](#1-システム概要)
2. [システム構成](#2-システム構成)
3. [機能仕様](#3-機能仕様)
4. [データ仕様](#4-データ仕様)
5. [API仕様](#5-api仕様)
6. [画面仕様](#6-画面仕様)
7. [認証・セキュリティ仕様](#7-認証セキュリティ仕様)
8. [自動計算仕様](#8-自動計算仕様)
9. [ccfolia連携仕様](#9-ccfolia連携仕様)
10. [環境・デプロイ仕様](#10-環境デプロイ仕様)
11. [非機能要件](#11-非機能要件)

---

## 1. システム概要

### 1.1 目的

TRPG「幻奏戦記 Ru/Li/Lu/Ra ノイシュタルト（HobbyJAPAN）」のプレイヤーが、英雄・歌姫・奏甲の各キャラクターシートをWebブラウザ上で作成・管理・共有できるシステムを提供する。

### 1.2 主要機能

| 機能 | 概要 |
|------|------|
| キャラクター管理 | 英雄・歌姫・奏甲シートの作成・編集・削除 |
| 自動計算 | 能力値入力に基づくHP/MP・戦闘修正値の自動算出 |
| 公開・共有 | キャラクターの公開設定と共有URLの発行 |
| ccfolia連携 | ccfolia（オンラインセッションツール）用JSONエクスポート |
| ユーザー認証 | アカウント登録・ログインによる個人データ管理 |

### 1.3 対象ユーザー

- 「幻奏戦記 Ru/Li/Lu/Ra ノイシュタルト」のプレイヤー
- TRPG オンラインセッション（ccfolia使用）プレイヤー

---

## 2. システム構成

### 2.1 技術スタック

| レイヤー | 技術 | バージョン |
|---------|------|-----------|
| バックエンド | Node.js | 18以上 |
| Webフレームワーク | Express | 4.18以上 |
| データベース（本番） | Turso（libSQL / SQLiteクラウド） | @libsql/client ^0.6.0 |
| データベース（ローカル） | SQLite（ローカルファイル） | @libsql/client経由 |
| 認証 | JWT（jsonwebtoken） | 9.0以上 |
| パスワードハッシュ | bcryptjs | 2.4以上 |
| フロントエンド | Vanilla HTML / CSS / JavaScript | — |
| ID生成 | uuid（v4） | 9.0以上 |

### 2.2 ファイル構成

```
rulilura/
├── src/
│   ├── server.js               # Expressサーバー本体・起動処理
│   ├── db/
│   │   └── database.js         # DB接続・テーブル初期化・クエリヘルパー
│   ├── middleware/
│   │   └── auth.js             # JWT認証ミドルウェア
│   └── routes/
│       ├── auth.js             # 認証API（登録・ログイン）
│       └── crud.js             # 英雄/歌姫/奏甲 CRUD APIファクトリ
├── public/
│   ├── index.html              # SPAエントリーポイント
│   ├── css/
│   │   └── style.css           # 全画面共通スタイル
│   └── js/
│       ├── gamedata.js         # ゲームデータ定数・ccfoliaコマンド生成
│       ├── api.js              # REST APIクライアント・認証状態管理
│       ├── hero-form.js        # 英雄シートフォームロジック
│       ├── singer-form.js      # 歌姫シートフォームロジック
│       ├── armor-form.js       # 奏甲シートフォームロジック
│       └── app.js              # メインアプリケーション・画面制御
├── data/                       # ローカルSQLiteファイル格納（自動生成）
├── package.json
├── render.yaml                 # Renderデプロイ設定
├── .gitignore
└── README.md
```

### 2.3 アーキテクチャ概要

```
ブラウザ（SPA）
  ↕ HTTP/REST API（JSON）
Expressサーバー（Node.js）
  ├── 静的ファイル配信（public/）
  ├── /api/auth      → 認証ルート
  ├── /api/heroes    → 英雄CRUDルート
  ├── /api/singers   → 歌姫CRUDルート
  └── /api/armors    → 奏甲CRUDルート
        ↕ @libsql/client
  Turso（本番）/ SQLiteローカルファイル（開発）
```

---

## 3. 機能仕様

### 3.1 ユーザー認証機能

#### 3.1.1 ユーザー登録

| 項目 | 仕様 |
|------|------|
| ユーザー名 | 英数字・アンダースコアのみ、3〜20文字 |
| パスワード | 6文字以上 |
| 重複チェック | 同名ユーザーが存在する場合は409エラー |
| パスワード保存 | bcryptjs（ソルトラウンド10）でハッシュ化して保存 |
| 登録成功時 | JWTトークン（有効期限30日）を返却 |

#### 3.1.2 ログイン

| 項目 | 仕様 |
|------|------|
| 認証方式 | ユーザー名＋パスワード照合 |
| 失敗時 | ユーザー名・パスワードどちらが誤りかは区別しない（セキュリティ上） |
| 成功時 | JWTトークン（有効期限30日）を返却 |
| トークン保存 | ブラウザのlocalStorageに保存 |

#### 3.1.3 ログアウト

localStorageからトークンを削除し、認証ページへ遷移する。

---

### 3.2 英雄シート管理機能

#### 3.2.1 入力項目

**基本情報**

| 項目 | 型 | 必須 | 説明 |
|------|-----|------|------|
| 英雄名 | 文字列 | ○ | キャラクター名 |
| 性別 | 選択（男性/女性） | — | — |
| 年齢 | 文字列 | — | — |
| 現世の国籍 | 文字列 | — | — |
| 現世の職業 | 文字列 | — | — |
| レベル | 数値（1〜20） | — | 英雄レベル |

**能力値**（各2〜20の範囲）

| 能力値 | 説明 |
|--------|------|
| 筋力 | 白兵ダメージ・HP計算に使用 |
| 器用さ | 射撃命中・HP計算・スキルポイントボーナスに使用 |
| 敏捷 | 回避・HP計算に使用 |
| 生命力 | 耐性・HP計算に使用 |
| 知力 | 射撃命中・MP計算・スキルポイントボーナスに使用 |
| 精神力 | 耐性・MP計算に使用 |

**スキル**（全36種、各0〜99）

戦闘系・近代戦系・現世系・アーカイア系・一般系の5カテゴリに分類。詳細はゲームデータ仕様（第4章）を参照。

**HP / MP**（自動計算・手動上書き可）

| 項目 | 自動計算式 |
|------|-----------|
| 通常HP | 筋力の値 ＋ 器用さの値 |
| 負傷HP | 敏捷の値 ＋ 生命力の値 |
| MP | 知力の値 ＋ 精神力の値 |

**戦闘修正**（自動計算・一部手動上書き可）

| 修正値 | 自動計算式 |
|--------|-----------|
| 白兵修正 | 筋力修正 ＋ 敏捷修正 |
| 射撃修正 | 器用さ修正 ＋ 知力修正 |
| 回避の元値 | 敏捷修正 ＋ 《回避》スキル値 － 防具の回避ペナルティ合計（自動計算・手動上書き可） |
| 回避修正 | 回避の元値の「1の位」を切り捨てて10単位にした値（自動計算・読み取り専用） |
| 抵抗修正 | 精神力修正 ＋ 生命力修正 |
| 防御値 | 防具の防御値合計（自動計算・手動上書き可） |
| ダメージ修正 | 手動入力 |

※修正値 ＝ 能力値 − 10

**回避修正の変換ルール（重要）**

回避の元値は「1の位を切り捨てて10単位にした値」を回避修正とする。負の値は0に向かう方向ではなく、0から離れる方向に切り捨てる。

| 回避の元値 | 回避修正 |
|-----------|---------|
| 1 〜 9 | 0 |
| 10 〜 19 | 10 |
| -1 〜 -10 | **-10**（0ではない） |
| -11 〜 -20 | **-20** |
| -14 | **-20** |

計算式：`Math.floor(元値 / 10) * 10`

**個人武器**（複数登録可）

| 項目 | 説明 |
|------|------|
| 武器名 | — |
| 命中値 | 基本命中値＋戦闘修正＋スキル値 |
| 武器スキル | 使用するスキル名 |
| ダメージ | ダイス式（例: 1D10+5） |
| 射程 | 白・近・中・長 |

**防具**（複数登録可）

武器と同様に行単位で複数登録する。テキスト自由記述方式は廃止。

| 項目 | 説明 |
|------|------|
| 防具名 | 自由入力（例: 皮ヨロイ、小盾） |
| 防御値 | 数値入力 |
| 回避ペナルティ | 数値入力 |

行を追加・編集するたびに防御値合計・回避ペナルティ合計を自動表示し、戦闘修正欄の「防御値」「回避の元値」にリアルタイムで反映する。

**英雄能力**（複数登録可）

ドロップダウンで全54種の英雄能力から選択して行単位で管理する。テキスト一括入力方式は廃止。

| 項目 | 型 | 説明 |
|------|-----|------|
| 能力名 | 選択（54種） | カタログ番号付きドロップダウンから選択 |
| 効果サマリー | 自動表示 | 選択した能力の効果を自動表示（読み取り専用） |
| メモ | 文字列 | 適用対象・回数・習得条件など自由記述 |

**英雄能力カタログ（全54種）**

| No | 能力名 | 効果サマリー |
|----|--------|------------|
| 1 | 能力アップ | 好きな能力値1つを最大+3上昇 |
| 2 | スキルUP | 指定スキルをただちに+8 |
| 3 | 十八番（おはこ） | 2D10MPで非戦闘スキルチェック+20 |
| 4 | インテリ | 1D10MPで知識スキルを一時習得 |
| 5 | 成長ボーナス | レベルアップ必要経験点を減らす |
| 6 | 根性 | 1シナリオ1回、MPを3D10回復 |
| 7 | 騎士 | 防具の回避ペナルティを軽減 |
| 8 | 寄生蟲バスター | 蟲への命中+10、ダメージ+4、奇声抵抗+20 |
| 9 | 局地戦エキスパート | 選んだ地形で命中・回避・隠蔽・偵察UP |
| 10 | 対空戦闘エキスパート | 対空射撃命中+20、回避修正を無効化 |
| 11 | 水中戦闘エキスパート | 水中で命中+10、水中ペナルティ無視 |
| 12 | 強打 | 2D10MPで白兵ダメージ+1D10 |
| 13 | 渾身の一撃 | 4D10MPで両手白兵ダメージ+2D10 |
| 14 | 集中攻撃Ⅰ | 2D10MPで追加1回攻撃（1戦闘1回） |
| 15 | 集中攻撃Ⅱ | 2D10MPで追加1回攻撃（何回でも） |
| 16 | 二挺射撃（個人） | 個人戦闘で両手射撃可（命中-30） |
| 17 | 二挺射撃（奏甲） | 奏甲戦でも二挺射撃可 |
| 18 | 二挺射撃（大型武器） | 両手射撃武器でも二挺射撃可 |
| 19 | 全力射 | 3D10MPで奏甲の全射撃武器で攻撃 |
| 20 | 奏甲猟兵Ⅰ | 奏甲からの追加ダメージ半減 |
| 21 | 奏甲猟兵Ⅱ | 奏甲からの追加ダメージ無し |
| 22 | 奏甲回避Ⅰ | 2D10MPで命中部位を1つずらす |
| 23 | 奏甲回避Ⅱ | 命中部位を上か下にずらす |
| 24 | 緊急射撃 | 2D10MPで使用条件を無視して武器使用 |
| 25 | 武器エキスパート | 全武器の知識要件を満たしているとみなす |
| 26 | 圧倒 | 4レベル以上低い敵への特殊効果 |
| 27 | 機先 | 6レベル以上低い敵の攻撃前に反撃可 |
| 28 | オーラ | 各種チェックでレベル+1扱い（最大3回） |
| 29 | 見切り | 6レベル以上低い敵からのダメージ半減 |
| 30 | 致命的一撃 | ゾロ目命中時に防御値を無視 |
| 31 | スナイパー | 1ターン照準か2D10MPで射撃命中+20 |
| 32 | ピンポイント攻撃 | 2D10MPでピンポイント攻撃可能 |
| 33 | レンジャー | 隠蔽状態を維持したまま攻撃可能 |
| 34 | 受け流し | 4D10MPで白兵命中を無効化し跳ね返す |
| 35 | 身代わり | 他キャラのダメージを防御値なしで肩代わり |
| 36 | 愛機 | 同一奏甲3冒険で命中・回避・ダメージにボーナス |
| 37 | 幻糸抵抗力 | 歌術・呪いへの抵抗修正+20 |
| 38 | 耐性 | 毒/病気/呪いへの抵抗を2回判定可 |
| 39 | 奇跡的回避 | 3D10MPで失敗した抵抗/回避をやり直し |
| 40 | 幸運 | 1冒険1回、振ったダイスをすべて振り直し |
| 41 | ダイハード | 死亡1回分を無効（1回使用で消滅） |
| 42 | 執念 | MP0になっても1ターンは気絶しない |
| 43 | MPの驚異的回復 | 休息時のMP回復量を大幅増加 |
| 44 | 主人公 | 仲間からMPをもらえる（応援） |
| 45 | 精神集中 | 根性でのMP回復を全MP全回復にできる |
| 46 | MP増加 | レベルアップ時のMP増加を2D10MPに |
| 47 | 歌姫強化 | 歌姫に能力を付与 |
| 48 | ユニゾン | 歌姫の能力修正2つを英雄に加える |
| 49 | 外道／鬼畜系英雄 | 絆ダメージを50%で却下できる |
| 50 | 懇願 | 1シナリオ1回、歌姫の消耗チェックを無効化 |
| 51 | 絆レベルアップ | 絆レベル+1（何回でも取得可） |
| 52 | 応急修理 | 2D10×20分で奏甲ダメージを半分に |
| 53 | チューンの天才 | チューン成功時にペナルティ無視可能 |
| 54 | 整備の天才 | 修理費か修理時間を半分にできる |

> **旧データ互換**: `abilities_memo`（テキスト形式）が存在する場合、編集画面で警告表示し参照しながら新形式へ移行できる。保存後は`hero_abilities`配列に統一され`abilities_memo`はクリアされる。

**テキスト項目**

| 項目 | 説明 |
|------|------|
| その他装備品 | 武器・防具以外の所持品（冒険者セット、歌術アイテムなど）を自由記述 |
| メモ | 自由メモ |

---

### 3.3 歌姫シート管理機能

#### 3.3.1 入力項目

**基本情報**

| 項目 | 型 | 必須 |
|------|-----|------|
| 歌姫名 | 文字列 | ○ |
| 年齢 | 文字列 | — |
| 出身国 | 文字列 | — |
| 歌姫レベル | 数値（1〜10） | — |
| 階位 | 数値（1〜12） | — |
| 絆レベル | 数値（1〜6） | — |

**能力値**：英雄シートと同様（6種）

**HP**

| 項目 | 自動計算式 |
|------|-----------|
| 通常HP | 筋力の値 ＋ 器用さの値 |
| 負傷HP | 敏捷の値 ＋ 生命力の値 |

※歌姫にMPはない

**消耗ゲージ**（肉体・気力・絆の3種）

| 項目 | 説明 |
|------|------|
| 消耗値 | 消耗チェックの成功目標値（2D10で以下なら成功） |
| 獲得値 | 絆獲得チェックの成功目標値 |
| ゲージ数 | ダメージボックスの数 |

肉体・気力の消耗値は能力値から自動計算（絆は固定：消耗値9・獲得値11）。

**消耗値自動計算表**

| 能力修正合計 | 消耗値 |
|-------------|--------|
| −7以下 | 6 |
| −6〜−4 | 7 |
| −3〜−1 | 8 |
| 0〜+2 | 9 |
| +3〜+5 | 10 |
| +6〜+8 | 11 |
| +9〜+11 | 12 |
| +12以上 | 13 |

※肉体消耗値 = 筋力修正＋生命力修正の合計を上記表で変換  
※気力消耗値 = 精神力修正＋知力修正の合計を上記表で変換

**スキル**（全36種、各0〜99）

英雄と同一の全36スキルから選択可能。以下の6スキルに初期値が設定される。

| スキル名 | 初期値 |
|---------|--------|
| アーカイア知識 | 20 |
| 歌術知識 | 15 |
| 蟲知識 | 15 |
| 回避 | 10 |
| 隠蔽 | 5 |
| 料理 | 5 |

表示順序はアーカイア系→戦闘系→近代戦系→現世系→一般系の順。

**歌姫能力**（個別行管理、63種）

ドロップダウンから歌姫能力を選択し、能力ごとにメモを記録する。

| 番号 | 能力名 | 効果サマリー |
|------|--------|-------------|
| 1 | 能力アップ | 好きな能力値1つに+3（作成時2倍） |
| 2 | スキルアップ | スキル3つの値に+5（作成時は5つ） |
| 3 | 歌姫レベルアップ | 歌姫レベル+1、好きな能力値+1、HP+5 |
| 4 | 階位上昇 | 階位+1（作成時2倍） |
| 5 | 絆レベルアップ | 絆レベル+1（作成時以外はコスト2倍） |
| 6 | 戦闘支援（ダメージ） | 武器スキル2つに+10、戦闘支援でダメージ増加 |
| 7 | 戦闘支援（命中値） | 武器スキル2つに+10、戦闘支援で命中値UP |
| 8 | 戦闘系歌姫 | 武器スキル2つに+10、単独で戦闘参加可 |
| 9 | 突撃娘 | 【筋力】+4、両手武器を片手で扱える |
| 10 | 不戦歌姫 | 戦闘参加で降伏か気絶、追加歌姫能力2個か英雄能力1つ授与 |
| 11〜63 | （以降省略） | カタログ参照 |

> **旧データ互換**: `abilities_memo`（テキスト形式）が存在する場合、編集画面で警告表示し参照しながら新形式へ移行できる。保存後は`singer_abilities`配列に統一され`abilities_memo`はクリアされる。

**歌術**（個別行管理、18種）

ドロップダウンから歌術を選択し、ランクとメモを記録する。

| フィールド | 型 | 説明 |
|-----------|-----|------|
| 歌術名 | 選択（18種） | ドロップダウンで選択 |
| ランク | 数値（R1〜R6） | 習得ランクを選択 |
| 効果 | 文字列（自動表示） | 歌術・ランク選択時に自動表示（読み取り専用） |
| メモ | 文字列 | コスト・対象・注意事項など |

**歌術カタログ（18種・各ランク効果付き）**

歌術選択後、ランクを選択すると効果テキストがフォーム上に自動表示される。

| 番号 | 歌術名 | 最大ランク | R1効果 | R2効果 | R3効果以降 |
|------|--------|-----------|--------|--------|-----------|
| 64 | 癒しの歌「花びらの子守唄」 | R4 | HP+2D10回復（ペア英雄は+絆LV） | 病気を治療 | R3:毒解除 / R4:呪い解除 |
| 65 | 個人戦闘補助の歌「早足のワルツ」 | R4 | 命中値+10 または HP+20 | 回避修正+10 | R3:防御値+[3+絆LV] / R4:全員適用 |
| 66 | 奏甲戦闘補助の歌「歌楽器ソナタ」 | R3 | 奏甲の白兵命中+10 | 射撃命中+10 または 回避修正+10 | R3:防御値+3 または 他機にも適用 |
| 67 | 打撃増加の歌「剣と盾のノクターン」 | R3 | 個人ダメージ+[3+絆LV] | 奏甲ダメージ+[3+絆LV] | R3:他1人/1機にも適用 |
| 68 | 身代わりの歌「偉大なる精霊の葬送曲」 | R4 | ペア英雄への攻撃を肩代わり | ペア奏甲への攻撃を肩代わり | R3:任意キャラへ / R4:任意奏甲へ |
| 69 | 探知の歌「大地の調べ」 | R5 | 周囲照明 または 幻糸の乱れ探知 | 歌術の存在を探知 | R3:1日前の足跡 / R4:透明存在 / R5:1週間前の足跡 |
| 70 | 蟲退治の歌「常しえの巨人の狂い歌」 | R4 | 全味方の奇声抵抗+20 | 奇声1種を打ち消す | R3:蟲に3D10ダメージ / R4:+麻痺 |
| 71 | 抵抗の歌「夢見る白馬のアリア」 | R4 | 本人とペア英雄の抵抗修正+20 | 可聴範囲全員の抵抗修正+20 | R3:歌術効果1つを無視 / R4:歌術1つ解除 |
| 72 | 特殊起動補助の歌「ザ・トッカータ」 | R4 | 特殊起動時【精神力】+1 | 障害モードペナルティ無視 | R3:起動延長と同効果 / R4:英雄なしで起動可 |
| 73 | リンクの歌「姫と英雄の二重奏」 | R4 | ペア英雄の方角・距離を感知 | テレパシーで会話可 | R3:絆レベル一時+1 / R4:絆レベル一時+2 |
| 74 | 奏甲補修の歌「彷徨い者のヒム」 | R4 | 奏甲部位HP0効果を半減 | 2部位のHPを回復 | R3:部位HP0効果1つ無視 / R4:維持費0 |
| 75 | 奏甲サポートの歌「ストラーマイン序曲」 | R5 | 移動速度UP（偶数ターン2回移動可） | 水中移動可能に | R3:地表1m浮遊 / R4:水上1m浮遊 / R5:効果8時間持続 |
| 76 | 結界の歌「小鳥のための小練習曲」 | R4 | 歌姫自身に防御値5の結界 | ペア英雄に防御値[絆LV+3] | R3:指定対象に防御値5 または 魔物への障壁 / R4:精霊を退散 |
| 77 | 転移とリンクの歌「金と銀と鋼の組曲」 | R5 | 接触対象の居場所を感知 | 2人でお互いに会話可 | R3:持続1D10日 または 念話 / R4:奏甲テレポート / R5:恒久的転移門 |
| 78 | 支配の歌（禁忌）「唾棄されし者どもの変奏曲」 | R4 | 接触対象の記憶を操作 | 生物1体を精神支配 | R3:持続24時間延長 / R4:3体同時支配 |
| 79 | 打撃の歌（禁忌）「神々の雄たけび」 | R6 | 生物1体に1D10ダメージ | 生物1体に2D10ダメージ | R3:2体2D10 / R4:全員2D10 / R5:1体4D10複数HP / R6:R5+回避修正-20 |
| 80 | 精神攻撃の歌（禁忌）「宮廷道化のサンバ」 | R4 | 気力消耗×2 または 2D10MPダメージ | 奇声効果1つをコピー | R3:気絶またはMP0 / R4:全員に効果 |
| 81 | 幻糸操作の歌（禁忌）「落ちて崩れし人形の前奏曲」 | R6 | 対象が話せなくなる | 歌姫を行動不能にする | R3:奏甲を障害モード化 / R4:奏甲機能停止 / R5:聖霊を破壊 / R6:歌姫を死亡 |

※フォーム上では歌術名・ランク・効果（自動表示・読み取り専用）・メモの4列で管理する。

> **旧データ互換**: `songs`（テキスト形式）が存在する場合、編集画面で警告表示し参照しながら新形式へ移行できる。保存後は`singer_songs`配列に統一され`songs`はクリアされる。

**戦闘系歌姫：武器・装備**（「戦闘支援（6/7番）」または「戦闘系歌姫（8番）」取得時のみ）

| 項目 | 型 | 説明 |
|------|-----|------|
| 武器 | 複数行 | 武器名・命中値・武器スキル・ダメージ・射程 |
| 防具・装備品 | 文字列 | 装備防具と防御値などを自由記述 |

武器の命中判定・ダメージのccfolia出力は英雄と同様に白兵修正・筋力修正を自動加算する。

**その他テキスト項目**

| 項目 | 説明 |
|------|------|
| NG行動 | 設定したNG行動（選択分） |
| メモ | 自由メモ |

---

### 3.4 奏甲シート管理機能

#### 3.4.1 入力項目

**基本情報**

| 項目 | 型 | 必須 |
|------|-----|------|
| 奏甲名称（愛称） | 文字列 | ○ |
| 機体名（型式） | 文字列 | — |
| TL | 数値（0〜6） | — |
| 工房 | 文字列 | — |

**戦闘修正**（英雄の値・奏甲修正・最終値の3列）

| 修正 | 計算式 |
|------|--------|
| 最終白兵 | 英雄の白兵修正 ＋ 奏甲の白兵修正 |
| 最終射撃 | 英雄の射撃修正 ＋ 奏甲の射撃修正 |
| 最終回避 | 英雄の回避修正 ＋ 奏甲の回避修正 |
| 最終抵抗 | 英雄の抵抗修正 ＋ 奏甲の抵抗修正 |
| 最終偵察 | 英雄の偵察スキル値 ＋ 奏甲の偵察修正 |

**奏甲HP**（3段階）

| 種別 | 0になった時の効果 |
|------|-----------------|
| 小破HP | 装備1つを破壊 |
| 中破HP | 乗員全員にダメージ |
| 大破HP | 機体破壊 |

**搭載武器**（複数登録可）

| 項目 | 説明 |
|------|------|
| 武器名 | — |
| 基本命中値 | カタログ記載値 |
| 戦闘修正 | 白兵/射撃修正 |
| 武器スキル名 | 使用スキル名 |
| スキル値 | スキルポイント |
| 最終命中値 | 基本命中値＋戦闘修正＋スキル値（自動計算） |
| ダメージ | ダイス式 |
| 射程 | 白・1〜4 |
| 攻撃回数 | — |
| 条件 | 歌術知識・現世武器知識の必要値 |
| 重さ | 荷重ポイント |

**テキスト項目**

| 項目 | 説明 |
|------|------|
| 特殊ルール | 機体固有の特殊能力・ルール |
| メモ | 自由メモ |

**★ 上級ルール項目**（タクティカル戦闘・奏甲管理ルール使用時）

| 項目 | 型 | 説明 |
|------|-----|------|
| 通常移動速度 | 文字列 | 通常時の移動力（例: `3`） |
| 戦闘移動速度 | 文字列 | 戦闘時の移動力（例: `1`） |
| 移動属性 | 選択 | 地上（通常）・地上（不整地）・地上（浮遊）・飛行・水中・水中/地上（通常）・水中/地上（不整地）・飛行/地上（通常）の8種 |
| 最大荷重 | 数値 | 搭載可能な武器・装備の合計荷重上限 |
| 使用荷重 | 数値（自動計算） | 搭載武器の重さ（weightフィールド）の合計値。武器追加・削除時にリアルタイム更新 |
| 残荷重 | 数値（自動計算） | 最大荷重 − 使用荷重 |
| 武装タイプ | 複数選択 | A（対空）・C（通常）・G（大型）・H（隠密）・L（長距離）・M（多目的）・S（小型）をチェックボックスで複数選択 |
| ハードポイント | 文字列 | 武器装備箇所（例: `手×2、肩×2、胴×2`） |
| 価格（G） | 文字列 | 購入価格（ゴルト）。例: `200万G` |
| 価格（FP） | 文字列 | 購入価格（陣営ポイント）。例: `100FP` |
| 維持費（G） | 文字列 | シナリオごとの維持費（ゴルト）。例: `25万G` |
| 維持費（FP） | 文字列 | シナリオごとの維持費（陣営ポイント）。例: `12FP` |

**命中部位表**（上級ルール・複数行登録可）

| 項目 | 型 | 説明 |
|------|-----|------|
| D%範囲 | 文字列 | ダイス出目範囲（例: `01〜54`） |
| 部位 | 文字列 | 部位名（例: `胴体`・`右腕`・`コクピット`） |
| 防御値 | 数値 | その部位の防御値 |
| HP | 文字列 | その部位のHP（数値または`特殊`） |
| 0になった時の効果 | 文字列 | 部位破壊時の効果（例: `機体破壊`・`右手装備不可`） |

「デフォルト部位を一括追加」ボタンにより、標準7部位（胴体・右腕・左腕・脚・装備・コクピット・頭部）のD%範囲テンプレートを自動挿入できる。

---

### 3.5 公開・共有機能

#### 3.5.1 公開設定

- 各キャラクターに「公開/非公開」フラグを設定可能
- 公開キャラクターは「公開一覧」ページに表示される（最新100件）
- 公開一覧はログインなしでも閲覧可能

#### 3.5.2 共有URL

- 各キャラクター作成時にUUIDv4の共有トークンを自動発行
- 共有URL形式: `https://{ホスト}/share/{type}/{token}`
- 共有URLにアクセスすると、公開/非公開設定に関わらずキャラクターを閲覧可能
- URLを知っている人のみアクセス可能（リンク共有方式）

---

### 3.6 ccfolia JSON出力機能

各キャラクタータイプに応じてccfoliaのキャラクターカードJSONを生成する。詳細は第9章を参照。

---

## 4. データ仕様

### 4.1 データベーススキーマ

#### usersテーブル

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| id | TEXT | PRIMARY KEY | UUIDv4 |
| username | TEXT | UNIQUE NOT NULL | ユーザー名 |
| password_hash | TEXT | NOT NULL | bcryptハッシュ |
| created_at | TEXT | NOT NULL | ISO8601形式 |

#### heroesテーブル

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| id | TEXT | PRIMARY KEY | UUIDv4 |
| user_id | TEXT | NOT NULL, FK→users | 所有ユーザー |
| is_public | INTEGER | NOT NULL DEFAULT 0 | 0=非公開, 1=公開 |
| share_token | TEXT | UNIQUE | 共有トークン（UUIDv4） |
| data | TEXT | NOT NULL | シートデータ（JSON文字列） |
| created_at | TEXT | NOT NULL | ISO8601形式 |
| updated_at | TEXT | NOT NULL | ISO8601形式 |

#### singersテーブル

heroesテーブルと同構造に加え以下のカラムを持つ：

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| hero_id | TEXT | FK→heroes | ペア英雄のID（任意） |

#### armorsテーブル

singersテーブルと同構造に加え以下のカラムを持つ：

| カラム | 型 | 制約 | 説明 |
|--------|-----|------|------|
| singer_id | TEXT | FK→singers | ペア歌姫のID（任意） |

### 4.2 dataカラムのJSONスキーマ

#### 英雄（heroes.data）

```json
{
  "name": "string",
  "gender": "男性 | 女性",
  "age": "string",
  "nationality": "string",
  "job": "string",
  "level": "number",
  "abilities": {
    "筋力": "number",
    "器用さ": "number",
    "敏捷": "number",
    "生命力": "number",
    "知力": "number",
    "精神力": "number"
  },
  "skills": {
    "[スキル名]": "number"
  },
  "hp": {
    "normal": "number",
    "injured": "number",
    "mp": "number"
  },
  "modifiers": {
    "melee": "number",
    "ranged": "number",
    "evasionRaw": "number（回避の元値：敏捷修正＋回避スキル－防具ペナルティ合計）",
    "evasion": "number（回避修正：evasionRawを10単位に切り捨てた値）",
    "resistance": "number",
    "defense": "number（防具の防御値合計が反映される）",
    "damage": "number"
  },
  "weapons": [
    {
      "name": "string",
      "hit": "number",
      "skill": "string",
      "damage": "string",
      "range": "string"
    }
  ],
  "armors": [
    {
      "name": "string",
      "defense": "number",
      "evasionPenalty": "number"
    }
  ],
  "hero_abilities": [
    {
      "no": "number",
      "name": "string",
      "memo": "string"
    }
  ],
  "equipment": "string（武器・防具以外の所持品）",
  "notes": "string",
  "abilities_memo": "string（旧形式互換フィールド・通常は空文字）"
}
```

#### 歌姫（singers.data）

```json
{
  "name": "string",
  "age": "string",
  "origin": "string",
  "level": "number",
  "rank": "number",
  "bond_level": "number",
  "abilities": { "筋力": "number", "器用さ": "number", "敏捷": "number", "生命力": "number", "知力": "number", "精神力": "number" },
  "skills": { "[スキル名]": "number" },
  "hp": { "normal": "number", "injured": "number" },
  "modifiers": {
    "melee": "number",
    "ranged": "number",
    "evasion": "number",
    "resistance": "number",
    "defense": "number"
  },
  "gauges": {
    "肉体": { "cost": "number", "gain": "number", "boxes": "number" },
    "気力": { "cost": "number", "gain": "number", "boxes": "number" },
    "絆":   { "cost": "number", "gain": "number", "boxes": "number" }
  },
  "singer_abilities": [
    { "no": "number", "name": "string", "memo": "string" }
  ],
  "singer_songs": [
    { "no": "number", "name": "string", "rank": "number", "memo": "string" }
  ],
  "weapons": [
    {
      "name": "string",
      "hit": "number",
      "skill": "string",
      "damage": "string",
      "range": "string"
    }
  ],
  "equipment": "string",
  "ng_actions": "string",
  "notes": "string",
  "abilities_memo": "string（旧形式互換・通常は空文字）",
  "songs": "string（旧形式互換・通常は空文字）"
}
```

#### 奏甲（armors.data）

```json
{
  "name": "string",
  "model": "string",
  "tl": "number",
  "workshop": "string",
  "hero_melee": "number",
  "hero_ranged": "number",
  "hero_evasion": "number",
  "hero_resistance": "number",
  "hero_recon": "number",
  "armor_melee": "number",
  "armor_ranged": "number",
  "armor_evasion": "number",
  "armor_resistance": "number",
  "armor_recon": "number",
  "armor_damage_mod": "number",
  "defense": "number",
  "hp": { "small": "number", "medium": "number", "large": "number" },
  "weapons": [
    {
      "name": "string",
      "baseHit": "number",
      "combatMod": "number",
      "skill": "string",
      "skillVal": "number",
      "finalHit": "number",
      "damage": "string",
      "range": "string",
      "count": "string",
      "condition": "string",
      "weight": "number"
    }
  ],
  "special_rules": "string",
  "notes": "string",
  "adv": {
    "move_normal": "string（通常移動速度。例: '3'）",
    "move_combat": "string（戦闘移動速度。例: '1'）",
    "move_type": "string（移動属性。例: '地上（通常）'）",
    "max_load": "number（最大荷重）",
    "used_load": "number（使用荷重・自動計算）",
    "weapon_types": ["string（武装タイプ: A/C/G/H/L/M/S）"],
    "hardpoints": "string（例: '手×2、肩×2、胴×2'）",
    "price_g": "string（価格G。例: '200万G'）",
    "price_fp": "string（価格FP。例: '100FP'）",
    "maintain_g": "string（維持費G。例: '25万G'）",
    "maintain_fp": "string（維持費FP。例: '12FP'）",
    "hit_locations": [
      {
        "range": "string（D%範囲。例: '01〜54'）",
        "part": "string（部位名。例: '胴体'）",
        "defense": "number（部位防御値）",
        "hp": "string（部位HP。数値または '特殊'）",
        "effect": "string（破壊効果。例: '機体破壊'）"
      }
    ]
  }
}
```

---

## 5. API仕様

### 5.1 認証API

#### POST /api/auth/register

ユーザー登録。

**リクエストボディ**
```json
{ "username": "string", "password": "string" }
```

**レスポンス（200）**
```json
{ "token": "string", "username": "string" }
```

**エラーレスポンス**

| ステータス | 条件 |
|-----------|------|
| 400 | バリデーションエラー |
| 409 | ユーザー名重複 |
| 500 | サーバーエラー |

#### POST /api/auth/login

ログイン。

**リクエストボディ**
```json
{ "username": "string", "password": "string" }
```

**レスポンス（200）**
```json
{ "token": "string", "username": "string" }
```

**エラーレスポンス**

| ステータス | 条件 |
|-----------|------|
| 400 | 入力不足 |
| 401 | 認証失敗 |

---

### 5.2 キャラクターCRUD API

以下のエンドポイントは英雄（`/api/heroes`）・歌姫（`/api/singers`）・奏甲（`/api/armors`）の3リソースで共通のパターンを持つ。

#### GET /{resource}

自分のキャラクター一覧を取得する。

**認証**: 必須（Bearer Token）  
**レスポンス**: キャラクターオブジェクトの配列（updated_at降順）

#### GET /{resource}/public

公開キャラクターの一覧を取得する（最新100件）。

**認証**: 不要  
**レスポンス**: キャラクターオブジェクトの配列（username付き）

#### GET /{resource}/share/:token

共有トークンでキャラクターを取得する。

**認証**: 不要  
**レスポンス**: キャラクターオブジェクト（username付き）  
**エラー**: 404（トークン不一致）

#### GET /{resource}/:id

IDでキャラクターを取得する。

**認証**: 任意（非公開データは所有者のみ取得可）  
**エラー**: 404（存在しない）、403（非公開かつ非所有者）

#### POST /{resource}

キャラクターを新規作成する。

**認証**: 必須  
**リクエストボディ**
```json
{
  "data": { ...シートデータ },
  "is_public": false,
  "hero_id": "uuid（singers/armorsのみ、任意）",
  "singer_id": "uuid（armorsのみ、任意）"
}
```
**レスポンス（201）**: 作成したキャラクターオブジェクト

#### PUT /{resource}/:id

キャラクターを更新する。

**認証**: 必須（所有者のみ）  
**リクエストボディ**: POSTと同様（部分更新可）  
**エラー**: 404（存在しない・非所有者）

#### DELETE /{resource}/:id

キャラクターを削除する。

**認証**: 必須（所有者のみ）  
**レスポンス**: `{ "ok": true }`  
**エラー**: 404（存在しない・非所有者）

### 5.3 レスポンス共通形式

```json
{
  "id": "uuid",
  "user_id": "uuid",
  "username": "string（JOIN時のみ）",
  "is_public": true,
  "share_token": "uuid",
  "data": { ...シートデータ },
  "hero_id": "uuid | null",
  "singer_id": "uuid | null",
  "created_at": "ISO8601",
  "updated_at": "ISO8601"
}
```

### 5.4 エラーレスポンス共通形式

```json
{ "error": "エラーメッセージ（日本語）" }
```

---

## 6. 画面仕様

### 6.1 画面一覧

全画面はモーダルではなく独立したページとして遷移する（SPA内のページ切り替え）。

| 画面ID | 名称 | 表示要素ID | 認証要否 |
|--------|------|-----------|---------|
| P-01 | 認証画面 | `page-auth` | 不要 |
| P-02 | マイページ | `page-mypage` | 必須 |
| P-03 | 公開一覧 | `page-public` | 不要 |
| P-04 | キャラクター詳細 | `page-detail` | 任意（非公開は所有者のみ） |
| P-05 | キャラクター作成・編集 | `page-edit` | 必須（所有者のみ編集可） |
| P-06 | ccfolia JSON出力 | `page-ccfolia` | 任意 |
| P-07 | 共有URL閲覧 | /share/{type}/{token} | 不要 |

### 6.2 認証画面（P-01）

- ログインタブ・新規登録タブの切り替え
- 未ログイン時に表示。ログイン済みの場合はマイページへリダイレクト

### 6.3 マイページ（P-02）

- 英雄・歌姫・奏甲の3タブ構成
- 各タブにキャラクターカード一覧を表示
- カード操作ボタン：詳細表示（カードクリック）・編集・公開切替・ccfolia出力・共有URL・削除
- カードクリックまたは編集ボタンから、対応するページ（P-04/P-05）へ遷移する

### 6.4 公開一覧（P-03）

- 全ユーザーの公開キャラクターを一覧表示（英雄・歌姫・奏甲混在）
- updated_at降順、最大100件
- カードクリックで詳細ページ（P-04）へ遷移

### 6.5 キャラクター詳細ページ（P-04）

- 全幅を使った専用ページとしてキャラクターの全項目を表示
- ページ上部に「← 戻る」ボタン（遷移元のページに戻る）
- 所有者の場合のみ「編集」「公開切替」「共有URL」「削除」ボタンを表示
- 「ccfolia」ボタンは所有者・閲覧者ともに表示し、押下でccfolia出力ページ（P-06）へ遷移
- 編集ページから保存した場合は本ページへ自動的に遷移する

### 6.6 キャラクター作成・編集ページ（P-05）

- 全幅を使った専用ページとしてフォーム全体を表示（最大幅900px、中央寄せ）
- ページ上部・下部（スティッキーフッター）の両方に「保存する」「キャンセル」ボタンを表示
- 「キャンセル」は遷移元のページ（マイページまたは詳細ページ）に戻る
- 保存成功時は自動的に詳細ページ（P-04）へ遷移する

### 6.7 ccfolia JSON出力ページ（P-06）

- 生成されたJSONを全幅で表示
- ページ上部に「← 戻る」「📋 コピー」ボタンを表示
- 「戻る」は詳細ページ（遷移元がある場合）またはマイページに戻る

### 6.8 キャラクターカード

| タイプ | 表示情報 |
|--------|---------|
| 英雄 | 名前・性別・国籍・職業・レベル・通常HP・MP・白兵修正 |
| 歌姫 | 名前・出身国・歌姫レベル・絆レベル・階位 |
| 奏甲 | 名前・型式・工房・TL・防御値・HP（小/中/大）・武器数 |

### 6.9 ページ遷移の履歴管理

- アプリ内に簡易的な「前のページ」記憶機構を持つ（`_prevPage`）
- 詳細ページ・編集ページ・ccfoliaページからの「戻る」操作は、直前にいたページ（マイページ／公開一覧／詳細ページ）に戻る
- ブラウザの戻る/進むボタン（History API）には対応しない。画面内の「戻る」ボタンを使用する

---

## 7. 認証・セキュリティ仕様

### 7.1 JWT認証

| 項目 | 値 |
|------|-----|
| アルゴリズム | HS256（jsonwebtoken デフォルト） |
| ペイロード | `{ id, username }` |
| 有効期限 | 30日 |
| 秘密鍵 | 環境変数 `JWT_SECRET`（未設定時はデフォルト値） |
| 送信方式 | リクエストヘッダー `Authorization: Bearer {token}` |
| クライアント保存 | localStorage |

### 7.2 アクセス制御

| 操作 | 条件 |
|------|------|
| 公開キャラクター閲覧 | 誰でも可 |
| 共有URL経由閲覧 | 誰でも可（URLを知っている場合） |
| 非公開キャラクター閲覧 | 所有者のみ |
| キャラクター作成 | ログイン必須 |
| キャラクター編集・削除 | 所有者のみ |

### 7.3 パスワードセキュリティ

- bcryptjsによるハッシュ化（ソルトラウンド10）
- パスワード原文はDBに保存しない
- ログイン失敗時のエラーメッセージはユーザー名・パスワードどちらが誤りかを区別しない

### 7.4 入力バリデーション

| 項目 | ルール |
|------|--------|
| ユーザー名 | `/^[a-zA-Z0-9_]+$/` 3〜20文字 |
| パスワード | 6文字以上 |
| リクエストボディ最大サイズ | 2MB |
| SQLインジェクション対策 | プリペアドステートメント（@libsql/clientのパラメータバインド）使用 |

---

## 8. 自動計算仕様

### 8.1 英雄シート自動計算

能力値入力時にリアルタイムで以下を更新する。

```
通常HP  = 筋力の値 + 器用さの値
負傷HP  = 敏捷の値 + 生命力の値
MP      = 知力の値 + 精神力の値

白兵修正   = (筋力の値 - 10) + (敏捷の値 - 10)
射撃修正   = (器用さの値 - 10) + (知力の値 - 10)
抵抗修正   = (精神力の値 - 10) + (生命力の値 - 10)

スキル成功値 = 50 + スキル値
能力チェック成功値 = 50 + (能力値 - 10)
```

### 8.2 歌姫シート自動計算

```
通常HP  = 筋力の値 + 器用さの値
負傷HP  = 敏捷の値 + 生命力の値

白兵修正   = (筋力の値 - 10) + (敏捷の値 - 10)
射撃修正   = (器用さの値 - 10) + (知力の値 - 10)
抵抗修正   = (精神力の値 - 10) + (生命力の値 - 10)

肉体消耗値 = 消耗値表[(筋力修正 + 生命力修正)の合計]
気力消耗値 = 消耗値表[(精神力修正 + 知力修正)の合計]
```

### 8.3 奏甲シート自動計算

```
最終白兵   = 英雄の白兵修正 + 奏甲の白兵修正
最終射撃   = 英雄の射撃修正 + 奏甲の射撃修正
最終回避   = 英雄の回避修正 + 奏甲の回避修正
最終抵抗   = 英雄の抵抗修正 + 奏甲の抵抗修正
最終偵察   = 英雄の偵察スキル値 + 奏甲の偵察修正

武器最終命中値 = 基本命中値 + 戦闘修正 + スキル値
```

---

## 9. ccfolia連携仕様

### 9.1 出力JSONフォーマット

ccfoliaのキャラクターカードインポート形式に準拠する。

```json
{
  "kind": "character",
  "data": {
    "name": "string",
    "memo": "string",
    "initiative": "number",
    "commands": "string（コマンドを改行区切りで連結）",
    "status": [
      { "label": "string", "value": "number", "max": "number" }
    ],
    "params": [
      { "label": "string", "value": "string" }
    ]
  }
}
```

### 9.2 英雄カードの生成仕様

**status（ステータスバー）**

| label | value / max |
|-------|------------|
| 通常HP | hp.normal |
| 負傷HP | hp.injured |
| MP | hp.mp |

**params（パラメータ表示）**

6つの能力値をそのまま表示。

**initiative（イニシアチブ）**

敏捷修正値（敏捷の値 − 10）

**commands（判定コマンド）**

以下を順に生成する：

1. 能力チェック（6種）: `1D100<={50+修正} [{能力名}チェック 成功値:{成功値}]`
2. スキルチェック（値>0のもの全件）: `1D100<={50+スキル値} [{スキル名} スキル{値}]`
3. 武器攻撃（登録武器全件）: `1D100<={命中値} [{武器名}命中]`
4. 武器ダメージ（登録武器全件）: `{ダイス式} [{武器名}ダメージ]`

### 9.3 歌姫カードの生成仕様

**status**

| label | value / max |
|-------|------------|
| 通常HP | hp.normal |
| 肉体ゲージ | gauges.肉体.boxes |
| 気力ゲージ | gauges.気力.boxes |
| 絆ゲージ | gauges.絆.boxes |

**params（パラメータ表示）**

6つの能力値をそのまま表示。

**memo（テキストメモ）**

以下の情報を整形して出力する。

| 項目 | 内容 |
|------|------|
| 基本情報 | 歌姫Lv・絆Lv・階位・HP |
| 歌姫能力 | 番号.能力名（メモ）形式で全件列挙 |
| 歌術 | Rランク 歌術名（メモ）形式で全件列挙 |
| 武器 | 武器名：命中値 ダメージ式 射程（武器がある場合のみ） |
| 装備 | 防具・装備品テキスト（ある場合のみ） |
| NG行動 | NG行動テキスト（ある場合のみ） |

**commands（判定コマンド）**

以下を順に生成する。

1. 消耗チェック（3種）: `2D10<={消耗値} [{ゲージ名}消耗チェック 消耗値:{値}]`
2. スキルチェック（値>0のもの全件）: `1D100<={50+スキル値} [{スキル名} スキル{値}]`
3. 特殊起動チェック: `2D10<={精神力の値} [特殊起動【精神力】チェック]`
4. 歌術コマンド（singer_songs全件）: `[{歌術名} R{ランク} / {メモ}]`
5. 武器攻撃コマンド（戦闘系歌姫のみ・weapons全件）:
   - 白兵武器: `1D100<={基本命中+白兵修正} [{武器名}命中 基本{値}+{白兵修正}(白兵修正)]`
   - 射撃武器: `1D100<={基本命中+射撃修正} [{武器名}命中 基本{値}+{射撃修正}(射撃修正)]`
6. 武器ダメージコマンド（戦闘系歌姫のみ）:
   - 白兵武器: `{ダメージ式}+{筋力修正} [{武器名} 筋力修正+{値}込み]`（筋力修正0の場合は修正なし）

### 9.4 奏甲カードの生成仕様

**status**

| label | value / max |
|-------|------------|
| 小破HP | hp.small |
| 中破HP | hp.medium |
| 大破HP | hp.large |

**params**

白兵・射撃・回避・抵抗・防御値の最終値を表示。

**commands**

搭載武器全件の攻撃コマンド＋ダメージコマンド。

---

## 10. 環境・デプロイ仕様

### 10.1 環境変数

| 変数名 | 必須 | デフォルト値 | 説明 |
|--------|------|------------|------|
| `TURSO_URL` | 本番のみ | `file:{絶対パス}/data/rulilura.db` | TursoのDB接続URL |
| `TURSO_AUTH_TOKEN` | 本番のみ | `undefined` | Tursoの認証トークン |
| `JWT_SECRET` | 推奨 | `rulilura-secret-change-in-production` | JWT署名秘密鍵 |
| `PORT` | 任意 | `3000` | サーバーポート番号 |
| `NODE_ENV` | 任意 | `development` | 実行環境 |

### 10.2 ローカル環境

| 項目 | 仕様 |
|------|------|
| 必要環境 | Node.js 18以上 |
| 起動コマンド | `npm start` |
| 開発コマンド | `npm run dev`（ファイル変更自動反映） |
| DBファイル | `./data/rulilura.db`（自動生成） |
| ポート | 3000（`PORT`環境変数で変更可） |

**Windows注意事項**

- `data`フォルダは自動作成される（`fs.mkdirSync`）
- DBパスはWindowsのバックスラッシュをスラッシュに自動変換して使用

### 10.3 本番環境（Render + Turso）

**Renderサービス設定**

| 項目 | 値 |
|------|-----|
| サービス種別 | Web Service |
| ランタイム | Node |
| ビルドコマンド | `npm install` |
| 起動コマンド | `npm start` |
| インスタンスタイプ | Free（無料プラン可） |
| Disk | 不要（Turso使用のため） |

**Tursoセットアップ**

```bash
# CLIインストール（macOS/Linux）
curl -sSfL https://get.tur.so/install.sh | bash

# DB作成・接続情報取得
turso auth login
turso db create rulilura-db
turso db show rulilura-db --url        # → TURSO_URL
turso db tokens create rulilura-db    # → TURSO_AUTH_TOKEN
```

---

## 11. 非機能要件

### 11.1 パフォーマンス

| 項目 | 目標値 |
|------|--------|
| API応答時間（通常） | 500ms以内 |
| 公開一覧取得上限 | 100件 |
| リクエストボディ上限 | 2MB |

### 11.2 ブラウザ対応

| ブラウザ | 対応 |
|---------|------|
| Google Chrome（最新版） | ○ |
| Mozilla Firefox（最新版） | ○ |
| Microsoft Edge（最新版） | ○ |
| Safari（最新版） | ○ |
| スマートフォン（iOS/Android） | ○（レスポンシブ対応） |

### 11.3 レスポンシブ対応

| ブレークポイント | レイアウト変更 |
|----------------|-------------|
| 600px以下 | カードグリッドを1列に変更・ヘッダー副題を非表示・モーダルパディング縮小 |

### 11.4 データ永続性

| 環境 | 永続性 |
|------|--------|
| ローカル（SQLiteファイル） | サーバー起動中は永続。ファイル削除で消失 |
| 本番（Turso） | Tursoのサービス稼働中は永続（無料枠の制限あり） |
| Render無料プラン（Diskなし） | 再デプロイ時にデータリセット（Turso使用で回避） |

### 11.5 制限事項

- ユーザー削除機能は非実装（管理者操作が必要）
- パスワードリセット機能は非実装
- メール認証は非実装
- 英雄・歌姫・奏甲のペアリング（リレーション）はIDで保持するが、UI上の連携表示は非実装
- 公開一覧の取得は最新100件に制限
- 英雄能力の旧形式データ（`abilities_memo`テキスト）は自動変換されない。編集画面から手動で新形式へ移行する必要がある
- 歌姫能力の旧形式データ（`abilities_memo`テキスト）・歌術の旧形式データ（`songs`テキスト）は自動変換されない。編集画面から手動で新形式へ移行する必要がある
- 奏甲上級ルール項目（命中部位表など）は任意入力であり、基本ルールのみのプレイ時は入力不要
- 戦闘系歌姫の武器・装備欄は任意入力。「戦闘支援（6/7番）」または「戦闘系歌姫（8番）」を取得していない歌姫は入力不要
